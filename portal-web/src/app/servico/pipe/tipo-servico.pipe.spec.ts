import { TipoServicoPipe } from './tipo-servico.pipe';

describe('TipoServicoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoServicoPipe();
    expect(pipe).toBeTruthy();
  });
});
